interface:
  display_name: "Plan to Delivery"
  short_description: "Keep one checkpoint from design to delivery"
  default_prompt: "Use $plan-to-delivery to orchestrate this system-level requirement through isolated workers, maintaining one source-grounded task checkpoint, projecting complete role-relevant context, and requiring independent review and final outcome evidence."
policy:
  allow_implicit_invocation: false
