interface:
  display_name: "Plan to Delivery"
  short_description: "Carry intent and exact design through delivery"
  default_prompt: "Use $plan-to-delivery to verify or resume a system-level delivery workflow, then keep user intent and the exact design aligned across isolated design, review, implementation, acceptance, and cleanup Workers."
policy:
  allow_implicit_invocation: false
