interface:
  display_name: "Plan to Delivery"
  short_description: "Carry intent and the current design through delivery"
  default_prompt: "Use $plan-to-delivery to verify or resume a system-level delivery workflow, then keep user intent and the identified current design aligned across isolated design, review, implementation, acceptance, and cleanup Workers."
policy:
  allow_implicit_invocation: false
