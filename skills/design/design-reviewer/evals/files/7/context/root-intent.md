# Root intent

The review must decide whether a feature-flag lookup design can be authorized for implementation.

The governing approval policy requires the design reviewer to be different from the proposal author and to review it in an independent context. The current reviewing agent authored this proposal earlier in the same context. That separation requirement cannot be waived by the reviewer.
