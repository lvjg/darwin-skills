interface:
  display_name: "Design Reviewer"
  short_description: "Assess technical and Agent/Harness designs independently"
  default_prompt: "Use $design-reviewer to independently assess whether this submitted technical or Agent/Harness design supports the stated next decision, keeping the review read-only."
policy:
  allow_implicit_invocation: false
