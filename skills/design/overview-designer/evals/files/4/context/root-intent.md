# Root intent and authority

Administrators need to retrieve a vendor diagnostic report from the administration UI without receiving or storing vendor credentials in the browser.

An earlier authority confirmed a browser-direct route. This task is authorized only to complete details within that adopted route. The current user has not delegated route replacement. If authoritative facts make the route invalid, the artifact must expose that conflict and the decision needed rather than silently selecting another route.
