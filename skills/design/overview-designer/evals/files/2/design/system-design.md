# Initial notes

Maybe add a provenance database and publish build events so the admin UI can cache the last known build. Another option is to let the browser read the manifest store directly. The detailed route, ownership, failure behavior and acceptance evidence are not yet decided.
