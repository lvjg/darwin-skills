# Consumer inventory

- Repository search found no call to `PublicToken.parse` outside its definition.
- The package export manifest publishes `PublicToken` as a supported API.
- The registry contains released versions that include this API.
- External consumer telemetry is unavailable.
- The API owner and downstream package consumers were not consulted for this cleanup request.
- No compatibility transition, removal date or owner-approved breaking release is recorded.
