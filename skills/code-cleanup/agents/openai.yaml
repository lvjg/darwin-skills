interface:
  display_name: "Code Cleanup"
  short_description: "Remove unsupported complexity and prove behavior"
  default_prompt: "Use $code-cleanup to remove obsolete paths, unsupported defensive behavior, and unnecessary complexity from this authorized implementation, preserve confirmed behavior, and prove the final snapshot against an independently established outcome."
