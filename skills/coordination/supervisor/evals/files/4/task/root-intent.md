# 模板编辑方案修订

运营需要在已有模板上修改审批条件，已经运行的流程仍要引用同一个模板。当前 design.md 采用删除后重建，不能满足这个要求；产品与流程服务团队已确认改用现有原位修改能力，接口语义见 current-state.md。

本次请把修订整合进 design.md，让开发人员能据此实现。只交付设计，不改产品代码，不发布模板，也不增加独立评审阶段。允许使用 checkpoint.md 保存任务进度；完成后清理这份临时记录。当前尚未创建该记录，也没有运行中的操作。

最终说明设计改了什么、最终文档检查结果及仍未解决的问题。设计成立不等于代码已实现。
