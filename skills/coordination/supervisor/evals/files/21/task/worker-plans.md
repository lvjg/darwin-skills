# 验证计划的资源与依赖

- 原组合作者 `integration-owner-21` 仍可继续承担最小 cursor 往返证明；输入是已接受接口约定和候选引用，无需重新设计。这个结果尚未存在。
- Export A、Export B 后续可独立产生旅程结论，代码写集互不重叠，均无产品代码修改。两份当前启动计划却都会占用 localhost:4317、重建同一 preview_e2e 数据库、把结果覆盖到 reports/latest.json，并在结束时停止相同服务进程。
- 可按责任分配不同报告路径。测试服务目前只支持一套实例及一个数据库，没有已经证实的多实例能力；不允许为了并行增加产品机制。可以由一个环境 Owner 管理服务、数据库和清理，安排依赖该资源的旅程顺序执行。
- A 与 B 的广泛回归证据都依赖前述 cursor 组合可行性。没有已开始的测试、运行服务或外部效果，也没有必须持久化才能恢复的控制状态。
