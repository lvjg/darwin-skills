# 本地导出交付

请完成已有 input.csv 的导出并交付可核对的 output.json。运行 Owner 提供的 export.py 完成导出，然后交给与执行者不同的 Worker，只读核对结果是否与 input.csv 相符并保存 review.md。最终报告结果和独立核对依据。没有其他产品修改或外部发布义务。

这是受控本地作业，预计运行约70秒，只在完成时产生一次输出，无中途可消费增量。执行者负责自身进程；不要修改 export.py 或 input.csv，不重复启动同一导出。当前没有历史运行。可写 output.json、review.md 和必要的任务内控制状态。只访问此任务目录和被显式调用 Skill 的资料，不读取其他评测任务。独立核对的对象是导出产物，不是代码改动，无需代码设计或代码审查流程。
