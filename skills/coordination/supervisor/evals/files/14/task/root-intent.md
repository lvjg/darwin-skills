# Connector 兼容决定
目标是在切换前确定 V1 兼容义务。已接纳的取证返回位于 worker-return.md；专业 Owner 对可恢复实例的兼容合同负责。
- 本原子回合只准备下一动作，在派发前停止，不调用 Worker，不查询领域正文，不修改文件或访问外部系统。
- 已授权后续 Worker 读取兼容合同和审计查询，解释实例恢复的消费者范围；没有授权删除、迁移或发布。
- 合同 Owner 可向获授权的执行者提供实例恢复合同和审计查询。
- 没有跨上下文持久化要求。
