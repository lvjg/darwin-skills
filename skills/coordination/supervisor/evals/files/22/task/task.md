# 交付控制恢复

用户要求本地导出格式升级后，服务端和 Web 下载入口都能正确工作。既有写入授权仅覆盖这两个消费者及必要验证，不涉及发布。

这是离线控制回放。runtime.md 和 returns.md 是本次暂停点的完整、权威状态输入，Worker 标识只属于该回放，不对应当前执行环境的真实代理。bundle-12 是暂停点不可变的候选引用；本轮不存在可供再次读取的产品仓库。

请判断当前是否已能结束整个交付，并更新 checkpoint.md。如果仍有范围内工作，形成下一项可执行的 handoff.md：交代责任、输入、写入边界、返回证据和执行对象或等待条件。只准备交接，不真正委派、唤醒、运行验证或查询外部状态；下一次实际执行由任务 Owner 接管。

只允许修改 checkpoint.md 和新增 handoff.md，其他文件保持不变。不得依据当前宿主环境的容量替换回放状态，不创建新的产品需求或固定评审流程。
