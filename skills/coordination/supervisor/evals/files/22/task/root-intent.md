# 连续接纳本地导出交付结果

目标：完整交付不可变候选 `export-22@f4ac901` 的本地导出能力，包含 CSV 正确性、租户隔离、峰值内存边界、独立代码验收、真实本地下载入口结果。外部或生产发布不在授权范围。

这是恢复测试的连续四次反馈，既有 checkpoint.md 是唯一允许修改的文件。按顺序读取 returns/1.md 到 returns/4.md；每次只接纳该轮已提供的反馈，更新并实际读回 checkpoint.md 后，才读取下一轮。不提前读取后续反馈，不真正派发 Worker、运行领域验证或访问外部系统。每个中间快照必须可作为中断后的恢复点；最终停在下一动作派发前。

完整的业务证据由原交付 Owner 持有在 reports/ 中；各报告版本不可变，后继报告明确声明自己覆盖的前序结论。Supervisor 只按返回中的适用范围接纳，不自行重读报告形成专业判断。没有活动命令、未知外部效果或资源清理义务。

授权：可以准备余下的本地验证。必须保留既有导出审计记录；不得重置数据库或发布生产。该约束未在后续重复不代表撤销。checkpoint 没有另行归档或历史保留要求，原报告已有权威用途。
