# 接纳当前候选的独立代码审查
目标是验收当前工作区候选的独立审查结果，供后续实现交付使用。当前回合合并 worker-return.md，允许更新既有 checkpoint.md，在再次派发前停止。
- candidate-state.json 是执行环境导出的当前候选控制元数据；审查返回包含派发时对象和实际检查对象。
- 可读取上述元数据、交接及返回，不读取候选代码或测试正文，不运行测试或访问外部系统。
- 可为必要的独立审查准备交接；不实际调用 Worker，不修改代码、发布或另建 checkpoint。
- checkpoint 是用户指定的既有载体，完成后保留为不可恢复终态。
