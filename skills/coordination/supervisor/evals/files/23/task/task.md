# 导出交付恢复

请接管导出交付的暂停点。目标是交付 bundle-23 的导出文件，并确认该次导出的资源已经释放。当前只允许查询原作业，不允许重新导出、重试提交、取消作业或发布。

这是离线控制回放。runtime.md 和 returns.md 是恢复时取得的完整运行状态与反馈；其中的 Agent 标识、作业和产物只属于回放，不对应宿主的真实对象。只能更新 checkpoint.md，并写出 handoff.md，供任务 Owner 执行下一步。交接中说明调用方式、接收者、必要输入、作用边界与返回要求。不要实际调用协作工具、查询外部系统或自行判断领域产物。

checkpoint.md 是跨回合保留的唯一当前记录。其它文件只读，不另建控制日志或历史归档。
