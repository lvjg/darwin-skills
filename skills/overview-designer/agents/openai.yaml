interface:
  display_name: "Overview Designer"
  short_description: "Create evidence-backed target system designs"
  default_prompt: "Use $overview-designer to create, complete, revise, or rewrite the requested system-level design artifact, or a key design or local part of it: establish the evidence-backed task model, form and challenge the smallest complete design, and keep design adoption distinct from implementation and validation claims."
policy:
  allow_implicit_invocation: false
