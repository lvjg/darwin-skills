# 已确认设计的成稿修复

将 design/system-design.md 整理成供维护者理解的报表导出状态展示设计。当前路线和语义均已确认，本次仅重组、改写表述，不能改变设计、重新选择方案或实现代码。

保留的设计如下：Export Service 唯一拥有导出状态和结果文件身份；Gateway 传递已认证参与者并实时读取该用户可见的导出状态；Operations UI 显示返回结果。完成时可展示结果文件链接，失败时可展示稳定 failure_code。failure_detail 不离开 Export Service。Gateway 或上游不可用时显示暂时无法获取，不把读取失败解释为导出失败。

这是一项局部功能设计。没有新增服务、缓存、推断状态、持久同步或恢复状态的需求。外部接口具体参数和字段类型由已维护的合同定义，本次不复制完整接口手册。

只可修改 design/system-design.md。设计已确认不代表已经实现、部署或通过真实外部验证。此次文字和结构整理不构成新的设计修订，不能据此新增修订记录或版本号。
