# Vendor Diagnostic Report Design

**Design status:** Confirmed

The administration browser calls the vendor API directly. The remaining design work is to decide where the browser stores the organization credential and how it bypasses cross-origin restrictions.
