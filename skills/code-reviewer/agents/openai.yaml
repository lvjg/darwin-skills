interface:
  display_name: "Code Reviewer"
  short_description: "Accept or reject an exact code candidate"
  default_prompt: "Use $code-reviewer to decide whether this exact code candidate is implemented, integrated, and evidenced well enough for my next action."
policy:
  allow_implicit_invocation: false
