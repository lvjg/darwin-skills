interface:
  display_name: "Code Reviewer"
  short_description: "Review a concrete implemented code change"
  default_prompt: "Use $code-reviewer to review this exact implemented code change and decide whether it is ready to accept."
policy:
  allow_implicit_invocation: false
