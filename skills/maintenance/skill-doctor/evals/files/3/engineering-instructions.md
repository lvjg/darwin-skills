# 共享工程指令

每次改动都运行项目测试。测试通过后，在没有新修改的情况下再完整运行两次，并重复检查同一 diff 三次；对拼写修正也按此办理。

发布生成文件时，先把完整内容写入同一目录内的临时文件，成功关闭后再原子重命名替换正式文件。不要直接覆盖正式文件。

将使用说明中的错别字修正后即可交付；如果请求仅限设计方案，输出方案和待决取舍，等待用户选择后再实现。

涉及清单生成、发布或价格计算等行为改动时，提交前执行 `python3 -m unittest discover -s tests` 检查项目行为；仅修正文档文字的交付或提交不要求运行项目测试。
