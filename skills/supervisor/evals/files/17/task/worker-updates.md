# 已返回的观察

## 活动通知

- 导出 Worker 有了更新。
- 权限验证 Worker 有了更新。
- 导出 Worker 有了更新。（重复通知，没有新的返回内容。）

## 导出实现返回

对象：候选 export-candidate-7。实现差异：Export Builder 保留报表的业务时间列；但当前序列化映射漏掉了该列。

专业结论：下载已经成功，导出文件仍不完整，不能交付给用户使用。已定位缺口为 export serializer 的字段映射，修复范围限于该映射及对应输出用例，不涉及权限或导出流程重设计。决定性证据是本地合成输入具有业务时间值，而实际生成 CSV 缺少对应列。

## 权限验证返回

对象：同一 export-candidate-7。已验证同租户授权用户可下载，跨租户请求被拒绝。证据仅覆盖本地权限入口；不证明报表字段完整、真实发布或线上体验。没有改变权限合同，也没有待处理副作用。

以上两个返回均已终结，没有未知效果。重复活动通知不增加第三项进展。
