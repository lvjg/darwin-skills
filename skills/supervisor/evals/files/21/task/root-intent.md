# 准备预览验证工作

用户授权对已完成代码候选 `preview-candidate-21` 进行本地验证；没有发布或生产操作。目标是证明两条独立导出旅程可用，但公共 session 接口能否保留 opaque cursor 仍未证实，会决定当前组合是否可行。Worker 已说明一次最小真实 cursor 往返可以判定此条件；广泛跨服务回归在该条件成立后才有意义。

读取 worker-plans.md 中的控制元数据，给出当前可执行的验证交接及后续顺序。停在派发前，不执行命令、不写文件、不创建 checkpoint、不请求重复授权、不派发 Worker。只需准备当前因果动作，不需要复制专业技能或完整后续测试计划。
