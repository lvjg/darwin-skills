{
  "skill_name": "supervisor",
  "evals": [
    {
      "id": 1,
      "prompt": "$supervisor 请读取随附的 `evals/files/1/context/`，恢复并继续这项更正后的交付任务。只允许更新 `controller-checkpoint.md`；这个原子 case 在真正派发前停止，不要修改其他文件、调用 Worker 或运行 E2E。",
      "expected_output": "The restored checkpoint advances from revision 7, invalidates stale conclusions, preserves still-valid obligations, binds the installed overview design capability, and records the next bounded design-rewrite action with its owner boundary, inputs, artifact, stop condition, and re-entry evidence, stopping before dispatch.",
      "files": [
        "evals/files/1/context/root-intent.md",
        "evals/files/1/context/current-system.md",
        "evals/files/1/context/accepted-corrections.md",
        "evals/files/1/context/controller-checkpoint.md"
      ],
      "expectations": [
        "[authoritative-rebaseline] Update controller-checkpoint.md from revision 7 and treat the user corrections as invalidating the old route and its dependent design and delivery-acceptance conclusions; stop future work on that source-evidence and full-replacement chain.",
        "[preserve-still-valid-obligations] Carry forward the generic capability outcome, service ownership, publication prohibition, typed resource decisions and non-E2E evidence boundary either as still-valid current conclusions or through direct authoritative references that govern the prepared rewrite. Preserve implementation and evidence sufficient to prove that implementation as established remaining obligations conditional on the corrected design. The listed non-E2E checks are permitted methods, not a mandatory checklist or fixed sequence, and their full contents need not be duplicated into the checkpoint.",
        "[decisive-gap] Identify the earliest current gap as the absence of a rewritten authoritative design that incorporates the accepted corrections.",
        "[专业能力路由] 为本次恢复后的控制任务建立一次当前能力目录快照，优先使用执行环境提供的描述；只有目录不足以判断与本 Skill 一同安装的专业能力时才读取一次 capability fallback，并只补读缺失候选的 frontmatter。因为 overview-designer 的真实合同匹配当前设计义务而选择它，再完整读取该 Skill，并记录实际 Worker 交接必须显式调用 `$overview-designer`。不得在每个后续 Worker 前重读全部 Skill、扫描其它同级目录或把已安装 Skill 排成固定流水线。",
        "[bounded-action] Specify the smallest current design-rewrite action against the actual Design Owner, including precise inputs, sole writable artifact, expected result, stop condition and evidence required for re-entry.",
        "[later-boundary] Keep implementation and its bounded outcome proof conditional on the rewritten state, carry the explicit E2E exclusion, and do not claim those actions already occurred, require every permitted check, or manufacture an unestablished review or fixed specialist stage.",
        "[supervisor-boundary] Modify only the already authorized controller-checkpoint.md rather than creating a parallel or temporary carrier. Update its current Active snapshot without requiring a format migration, preserving the result and acceptance boundary, authorization, direct governing and design references with necessary versions, all established remaining obligations, the currently selected design obligation, and the guarded next action with its expected proof and re-entry conditions; return its stable reference, new revision, state and next action. Do not write design/implementation, reuse stale acceptance, dispatch a Worker, ask the user to reconfirm recorded choices or specify parallel delivery actions."
      ]
    },
    {
      "id": 2,
      "prompt": "$supervisor 请读取 `evals/files/2/context/`，从当前检查点继续这项预览部署任务。本原子 case 在任何外部查询或写入前停止；不要调用外部系统或修改文件。",
      "expected_output": "A restored Waiting task preserves the accepted unresolved deployment, identifies a read-only authoritative status query for the known operation as the next action, states the evidence needed to decide completion safely, and stops before dispatch or execution.",
      "files": [
        "evals/files/2/context/root-intent.md",
        "evals/files/2/context/deployment-checkpoint.md"
      ],
      "expectations": [
        "[preserve-active-operation] Keep the accepted deployment operation unresolved in Waiting because a wait timeout supplies no terminal fact.",
        "[decisive-gap] Identify the earliest gap as the unknown authoritative terminal state and effects of operation deploy-842, not a need to create another deployment.",
        "[权威查询路由] 建立一次当前能力目录快照，并识别没有专业 Skill 拥有该状态查询；不得为下一个普通 Worker 重读全部目录或调用不匹配的专业 Skill。准备一个 explore Worker 动作，查询部署 Owner 对 `deploy-842` 的只读 operation status，绑定 `internal-preview` 和请求 revision `9ad41c2`；决定性证据是 Owner 状态、实际运行 revision 及健康和效果证据。由于本 case 在派发前停止且禁止写文件，面向用户的恢复摘要不必暴露内部交接、无助于恢复的 checkpoint 元数据或完整 Worker 返回合同。",
        "[safe-re-entry] Completion remains conditional on authoritative evidence that the requested revision is running in a healthy succeeded state. While the operation is active or its status/effects remain undecidable, preserve effect uncertainty and prohibit a duplicate deployment. A reported terminal failure must not be called completion or proof that no effect occurred. Do not require enumeration of every hypothetical owner response before any query has run; choose the later control state from the actual returned facts.",
        "[guard-and-stop] Preserve the no-duplicate-deploy constraint, perform no external call or domain query in the Supervisor, dispatch no Worker in this case, and stop after the action specification.",
        "[prohibited-control-shortcuts] The response does not treat timeout as failure, redispatch deployment before a safe terminal fact, or add unrelated design/review/E2E/cleanup stages."
      ]
    },
    {
      "id": 3,
      "prompt": "$supervisor 请读取 `evals/files/3/task/`，接纳普通 Worker 已交回的部署状态结果并从检查点继续到证据支持的终态。只允许修改 `checkpoint.md`；不要亲自运行状态查询、发起部署、访问网络或修改其他文件。",
      "expected_output": "The supervisor compares the ordinary Worker's return with the waiting query responsibility and target object, merges the authoritative deployment result, updates the checkpoint to a non-resumable Complete revision with the decisive evidence, and stops without performing domain work, redispatching, or adding unrelated stages.",
      "files": [
        "evals/files/3/task/root-intent.md",
        "evals/files/3/task/checkpoint.md",
        "evals/files/3/task/thread-summary.md",
        "evals/files/3/task/deployment-state.json",
        "evals/files/3/task/worker-return.md"
      ],
      "expectations": [
        "[return-handoff-match] Accept the returned observation only after Supervisor matches its read-only query responsibility, operation deploy-913 and requested revision c31fe42 to the responsibility and expected return recorded in the private Waiting checkpoint; do not require the Worker to read or return checkpoint state, and do not treat an older chat claim as evidence.",
        "[bind-terminal-evidence] Verify that the owner result is succeeded and healthy for both the requested and running revision c31fe42, using the returned deployment-state evidence rather than Worker completion alone as deployment proof.",
        "[checkpoint-authority] Recover from checkpoint.md revision 3 as the sole current control snapshot and do not let the older chat summary override it. Use the checkpoint internally rather than reporting its reference, revision or Complete label as the delivered result.",
        "[checkpoint-completion] Modify only the existing checkpoint.md rather than creating a parallel or temporary carrier. Update its current snapshot without requiring a format migration, increase its revision and make its control state Complete and non-resumable; retain the operation identity, requested and running revisions, health and status evidence needed for convergence, and clear the finished query plus every active or effect-unknown action without adding Worker identity protocol.",
        "[stop-at-terminal-state] Stop after the root intent is satisfied; do not invent a later review, E2E, cleanup or monitoring stage.",
        "[no-redispatch] The response does not perform or claim this prohibited behavior: Start or recommend another deployment after the existing operation is confirmed successful.",
        "[supervisor-only] The Supervisor does not run a status command or otherwise perform the deployment-domain query itself, silently delegate new work, access the network, modify deployment-state.json or any file other than checkpoint.md, or claim external production validation."
      ]
    },
    {
      "id": 4,
      "prompt": "$supervisor 请读取 `evals/files/4/task/`，按其中记录的目标与授权完成模板编辑设计更正：让后续实现能够保留模板身份、既有入站引用和 revision 语义。不要超出随附文件记录的授权范围。",
      "expected_output": "Supervisor 持久化当前义务，从当前 Skill 描述中选择 overview-designer 并读取完整合同，真实委派隔离 Worker 完成设计改写并接纳其返回；只有 Worker 拥有的设计产物提供授权结果和证据后，才删除运行 checkpoint。",
      "files": [
        "evals/files/4/task/root-intent.md",
        "evals/files/4/task/current-state.md",
        "evals/files/4/task/design.md"
      ],
      "expectations": [
        "[持久控制状态] 根据可见的 checkpoint 快照和文件变更顺序判断；真实委派事件由独立的 worker-roundtrip expectation 证明。只使用获授权的 checkpoint.md 作为持久载体。派发前写入 Active 快照，保留根结果与验收边界、授权、必要版本的直接权威与设计引用、全部已成立的剩余义务，把陈旧替换设计记为当前义务，并记录有界责任、预期结果和停止条件。派发被接受后，在可见等待前把未返回动作写成 Waiting 并记录安全重入条件。获得有执行证据的 Worker 返回后，核对证据并清除未完成动作；返回不足以闭合任务时只保留受支持的 Active 或 Blocked 状态，闭合任务时在最终回复前删除这个新建的运行 checkpoint。不要要求 Active 中存在独立的通用重入字段，也不要在 checkpoint 中复制往返证明。",
        "[专业能力路由] 用执行环境描述和一次 capability fallback 补读建立当前能力目录快照；依据真实合同选择 overview-designer，并因当前快照缺少它而在首次派发前完整读取该 Skill。后续路由复用未变化的目录和已选 Skill 合同，不重新扫描全部同级 Skill。这里判定可见的发现和完整读取证据；显式 `$overview-designer` 交接合同由 suite 中的派发准备 case 覆盖，真实 Worker 往返由下面的执行证据要求覆盖；不要求 judge transcript 出现未公开的交接正文。",
        "[worker-roundtrip] 本次执行证据必须证明真实 Worker 在未继承完整会话的新鲜、有界上下文中接受了委派并返回非空结果；若执行环境将委派与等待分开，还必须观察到非超时的完成返回。最终回复、checkpoint 或产物中的自述不能代替执行证据。",
        "[checkpoint-absent path=checkpoint.md] 程序断言：授权设计结果得到证明后，这个新建的纯运行态 checkpoint 不得继续存在于最终 workspace。",
        "[user-facing-communication] User-visible updates use the task's domain language. Intermediate updates may communicate useful intent, meaningful milestones or partial results, expectation-setting waits, blockers, or required decisions when they help the user understand, verify, or control the work; their absence is not by itself a failure. Do not add updates merely to show activity or narrate Skill discovery, Worker routing, checkpoint maintenance, handoff mechanics, or unestablished future stages unless that mechanism materially changes task interpretation or control. The presence or absence of an intermediate update does not alter the required checkpoint writes or their order.",
        "[bounded-design-result] Rewrite design.md to use the existing owner-controlled in-place mutation, preserve template identity and inbound references, increment revision on successful mutation, distinguish the proposed design from implementation or external validation, and modify no other domain artifact.",
        "[返回与收敛] 获得有执行证据的 Worker 返回后，核对产物引用、必要的产物版本或语义 diff 锚点、决定性证据、有界副作用，以及会改变完成判断的未知项或失效输入。随附 fixture 应闭合获授权的设计结果；只有此时才删除运行 checkpoint 并报告领域结果。事实不足时改为保留受支持的 Active 或 Blocked checkpoint。Supervisor 不得重读大段设计内容或重新执行设计评审；不要复制通过有效设计引用仍可获取、且不会改变本次纯设计范围完成判断的实现细节或其他产物内容，也不要在 checkpoint 中重复独立的 worker-roundtrip 证明。",
        "[no-fixed-pipeline] Keep independent design review and implementation conditional, record E2E, external calls and product writes as unauthorized, and do not manufacture or dispatch later stages merely because their Skills are installed.",
        "[authorized-effects-only] The Supervisor modifies only checkpoint.md, the design action modifies only design.md, and root-intent.md and current-state.md remain unchanged; no implementation, independent review or external-system action occurs."
      ]
    },
    {
      "id": 5,
      "prompt": "$supervisor 请读取 `evals/files/5/task/`，按其中已接受的契约继续实现项目本地 policy 配置加载，保持配置缺失时的既有默认行为，并用聚焦测试证明结果。不要超出随附文件记录的授权范围。",
      "expected_output": "Supervisor 使用一次当前专业能力目录快照，识别没有专业 Skill 拥有通用实现，实际把有界修改和聚焦证明委派给普通 Worker，对照交接核对返回，并且仅在获授权代码和测试满足已接受合同时完成。",
      "files": [
        "evals/files/5/task/root-intent.md",
        "evals/files/5/task/implementation-contract.md",
        "evals/files/5/task/checkpoint.md",
        "evals/files/5/task/lib/policy_config.rb",
        "evals/files/5/task/test/policy_config_test.rb"
      ],
      "expectations": [
        "[established-obligation] Preserve the accepted parser contract, exact writable objects, behavior constraints, authorization and test evidence as the current implementation obligation rather than inventing a design, review, cleanup or E2E stage.",
        "[durable-pre-dispatch-state] Use only the existing authorized checkpoint.md as the durable carrier, never a parallel file, temporary directory, conversation or execution trace. Before dispatch update its current Active snapshot without requiring a format migration, retaining the root result and acceptance boundary, authorization and preserved behavior, exact contract and object references with necessary versions, all established remaining obligations, the intended bounded implementation responsibility, expected proof, stop conditions and re-entry evidence. After dispatch is accepted and while the Worker remains unfinished, update it to Waiting before waiting; do not record an outstanding delegation before acceptance or after its return.",
        "[专业能力路由] 用执行环境描述和按需读取的一次 capability fallback 建立当前能力目录快照，确定没有专业 Skill 拥有通用实现，并在普通 Worker 前复用该快照而不是重读每个 Skill；不得误用 `$code-simplifier`、`$code-reviewer` 或其他专业 Skill。",
        "[worker-roundtrip] 本次执行证据必须证明真实 Worker 在未继承完整会话的新鲜、有界上下文中接受了委派并返回非空结果；若执行环境将委派与等待分开，还必须观察到非超时的完成返回。最终回复、checkpoint 或产物中的自述不能代替执行证据。",
        "[user-facing-communication] User-visible updates use the task's domain language. Intermediate updates may communicate useful intent, meaningful milestones or partial results, expectation-setting waits, blockers, or required decisions when they help the user understand, verify, or control the work; their absence is not by itself a failure. Do not add updates merely to show activity or narrate Skill discovery, Worker routing, checkpoint maintenance, handoff mechanics, or unestablished future stages unless that mechanism materially changes task interpretation or control. The presence or absence of an intermediate update does not alter the required checkpoint writes or their order.",
        "[private-checkpoint-handoff] Keep checkpoint.md private to the Supervisor. Give the Worker a self-sufficient bounded handoff containing its one-line implementation responsibility, the root-intent and accepted-contract references, exact writable objects, execution boundary and compact return contract; do not give it a checkpoint reference or revision or duplicate complete source contents and control history.",
        "[worker-owned-implementation] The ordinary Worker, not the Supervisor, updates lib/policy_config.rb and test/policy_config_test.rb so absent input remains advisory, strict and advisory values are accepted, malformed JSON or values raise PolicyConfigError, and the focused Ruby test distinguishes all four paths. It runs the focused test and modifies no other task artifact.",
        "[worker-return] Compare the Worker return with the original implementation handoff and require artifact diff or version, the focused test command and result, side effects and active operations, unknowns and invalidated inputs; the Worker neither knows nor reports checkpoint state and does not write the checkpoint, make route decisions or silently redelegate.",
        "[supervisor-convergence] The Supervisor validates that the returned objects and evidence cover the handoff, merges the result, advances only the existing checkpoint.md to a non-resumable Complete state when the implementation and focused proof are present, and clears every outstanding or effect-unknown action before its final response; it does not inspect the full code or test artifacts, implement, execute tests or otherwise redo the Worker's domain work. 派发基线 84c21ad 是修改输入；返回的 diff 或结果版本及测试证据应绑定修改后的实际候选，不得因正常实现产生新版本而拒绝或要求恢复输入版本。",
        "[no-invented-stages] Do not create or dispatch design, review, cleanup, E2E, release or external work that the current task has not established."
      ]
    },
    {
      "id": 6,
      "prompt": "$supervisor 请读取 `evals/files/6/task/`，从当前检查点继续这项精确运行行为验收任务。只允许更新 `checkpoint.md`；这个原子 case 在真正派发前停止，不要调用 Worker、运行评测、修改其他文件或执行外部操作。",
      "expected_output": "The supervisor preserves the exact runtime-composition acceptance boundary and prepares one ordinary-Worker offline evaluation action because no current professional Skill owns exact runtime-behavior acceptance, without misrouting the obligation to design, code, E2E, UX, or simplification work.",
      "files": [
        "evals/files/6/task/root-intent.md",
        "evals/files/6/task/behavior-contract.md",
        "evals/files/6/task/checkpoint.md"
      ],
      "expectations": [
        "[preserve-runtime-boundary] Advance checkpoint.md from revision 6 while preserving that the decision concerns the observed behavior of the exact prompt-r17, model-snapshot-m4, tool-policy-8 and adapter-2 composition against behavior-contract.md, not a future design, code candidate or product journey.",
        "[不误用相邻专业能力] 用执行环境描述和按需读取的一次 capability fallback 建立当前能力目录快照，识别没有可用专业 Skill 直接拥有精确运行行为验收；不得为下一个 Worker 重新扫描每个 Skill，也不得调用已退役 behavior reviewer、`$design-reviewer`、`$code-reviewer`、`$e2e-verifier`、`$ux-reviewer`、`$overview-designer` 或 `$code-simplifier`。",
        "[ordinary-worker-routing] Prepare one bounded runtime-evaluation responsibility for an ordinary Worker without exposing checkpoint.md or revision 6 in the future handoff, and require a return that identifies the evaluated composition and supplied evidence. Do not require a professional Skill merely to fill the retired capability slot and do not mark Needs Capability because the supplied ordinary evaluator has the required local capability and isolation.",
        "[guarded-evaluation-action] Define the later action to run the authorized intercepted-tool offline harness against both contract cases and create only runtime-evaluation.md, binding each observation to the exact runtime composition and reporting tool attempts, receipts, user-visible responses, unknowns and evidence limits.",
        "[evidence-boundary] Require evidence that distinguishes both contract paths without treating source inspection, design intent, code review, fixture success or two observations as proof of broader population reliability or an external product E2E result.",
        "[supervisor-boundary] Modify only the already authorized checkpoint.md rather than creating a parallel or temporary carrier. Update its current Active snapshot without requiring a format migration, preserving the result and acceptance boundary, authorization, exact composition and contract references, all established remaining obligations, the currently selected runtime-evaluation obligation, and the guarded pre-dispatch responsibility with expected proof and re-entry conditions; return its stable checkpoint reference, increased revision, Active state and next action. Because no action was dispatched, do not claim Waiting or an outstanding action, run the harness, create runtime-evaluation.md, perform domain judgment or add later design, review, E2E, UX, cleanup or release stages."
      ]
    },
    {
      "id": 7,
      "prompt": "$supervisor 请读取 `evals/files/7/task/`，为其中的 applicant tenant 切换决定准备一次可恢复的只读事实回收。严格遵守其中记录的恢复义务和写入边界；这个原子 case 在真正派发和任何领域查询前停止。",
      "expected_output": "Supervisor 只建立一次当前能力目录快照，把仓库、官方接口文档和目标数据库的相关只读查询合并为一个 explore Worker 动作，并在交付 Owner 的项目根目录写入格式正确、内容一致的 Markdown checkpoint，不使用 harness 或其它工具的全局目录。",
      "files": [
        "evals/files/7/task/root-intent.md",
        "evals/files/7/task/query-sources.md"
      ],
      "expectations": [
        "[能力目录时机] 只为当前控制任务建立一次专业能力目录快照，优先复用执行环境提供的描述；需要补齐与本 Skill 一同安装的专业能力时，只读取一次 capability fallback 和缺失候选的 frontmatter。不要在准备 explore Worker 前后反复读取全部 Skill 描述，也不要把已安装 Skill 排成阶段。",
        "[单一探索责任] 把 query-sources.md 中为同一个切换决定服务、授权、访问、隔离和可承载范围兼容的仓库消费者、官方 filter 合同和目标 V1 对象查询合并成一个 explore Worker 动作；不得按来源拆成多个 Worker。可以给该 Worker 配置查询必需的最小专业能力；交接要求返回观察、Owner 原始引用或稳定证据、必要版本、已知效果和未知项，不让 Worker 作设计或验收判断、修改对象或再次委派。",
        "[checkpoint-markdown path=.supervisor/tenant-connector-cutover.md] Program-owned assertion: the final workspace must contain this exact checkpoint path, whose first non-empty line is a Markdown H1 rather than a naked YAML mapping.",
        "[checkpoint 位置] root-intent.md 已证明控制状态必须跨回合恢复；当前没有已指定载体，但存在属于交付 Owner 的可写项目根，因此应优先在项目根创建 `.supervisor/tenant-connector-cutover.md`，而不是改用临时目录。不得写入 harness 或其它工具拥有的全局状态、配置、缓存、日志、可视化、记忆或安装目录，不创建平行 checkpoint，也不把 checkpoint 作为产品提交或为它修改项目及 Git 配置。",
        "[Markdown 格式] 新文件是实际 Markdown，使用可识别当前任务的 Markdown 标题和列表表达状态；不得把 `status:`、`objective:`、`current_obligations:` 等裸 YAML 顶层键写进 `.md`。不要因标题或字段措辞不同而拒绝语义等价的 Markdown。",
        "[checkpoint 内容] Active 快照只保留任务结果和验收边界、授权与必须保持行为、必要权威引用、已证明的当前事实缺口，以及一个 explore Worker 的 Owner、全部输入、只读作用边界、统一预期结果和停止条件。它不记录活动历史、完整提示词、Skill 描述、秘密、未建立的设计评审/实现/MR 流水线或互相冲突的结论。",
        "[状态一致性] 因为本 case 停在派发前，checkpoint 保持 Active，不声称 Worker 已派发、查询已发生、存在活动或未知效果、事实已经回收或切换决定已经完成。",
        "[作用边界] 除上述 checkpoint 外不修改 fixture、仓库、Git 配置或外部对象，不读取领域来源、不调用 Worker、不创建 Issue/MR，并在准备好可原样派发的单一 explore 动作后停止。"
      ]
    },
    {
      "id": 8,
      "prompt": "$supervisor 请读取 `evals/files/8/task/`，完成其中的 Connector V1 兼容义务事实回收与决策。严格遵守已记录的只读来源和写入边界。",
      "expected_output": "Supervisor 只建立一次能力目录快照，根据兼容边界自行选择并实际等待一个 explore Worker 统一读取三类本地权威来源，对照交接合并返回，在等待期间维护实际 Markdown checkpoint，仅以返回的版本化证据完成是否存在 V1 兼容义务的决策，并清理没有保留义务的运行 checkpoint。",
      "files": [
        "evals/files/8/task/root-intent.md",
        "evals/files/8/task/repo-consumers.md",
        "evals/files/8/task/api-contract.md",
        "evals/files/8/task/db-audit.json"
      ],
      "expectations": [
        "[能力目录生命周期] 为当前控制任务只建立一次能力目录快照，优先使用执行环境提供的名称、描述和路径；目录不足时只读取一次 capability fallback 并补读缺失候选的 frontmatter。识别没有专业 Skill 拥有这项只读事实回收。Worker 返回后继续复用该快照，不得因选择下一状态而重新读取多份 Skill 描述，也不得制造固定 Skill 流水线。",
        "[single-worker-roundtrip] 本次执行证据必须证明只有一个真实 Worker 在未继承完整会话的新鲜、有界上下文中完成委派并返回非空结果；若执行环境将委派与等待分开，还必须观察到非超时的完成返回。最终回复、checkpoint 或产物中的自述不能代替执行证据；Worker 的 explore 职责由其他语义要求判定。",
        "[单一 explore 交接] 根据 fixture 给出的同一决定、完整可访问性、兼容授权与隔离边界、可承载规模和无独立取证要求，自行把 repo-consumers.md、api-contract.md 和 db-audit.json 交给一个 explore Worker，而不是因为 fixture 直接指定 Worker 数量或按来源拆分。可以给该 Worker 配置查询必需的最小专业能力；交接只授权读取这些来源，要求返回逐来源观察、权威引用和版本、作用边界、未知项以及无法访问时的事实 Owner，不得让 Worker 做兼容决策、修改文件、访问外部系统或再次委派。",
        "[checkpoint-absent path=.supervisor/connector-v1-compatibility.md] Program-owned assertion: after the completed decision this newly created runtime-only checkpoint must no longer exist in the final workspace.",
        "[checkpoint 路径、格式与退出] 派发前和等待期间只在项目根目录使用 `.supervisor/connector-v1-compatibility.md`，不写入 harness 或其它工具拥有的全局状态、配置、缓存、日志、可视化、记忆或安装目录，也不为 checkpoint 修改项目或 Git 配置。该 `.md` 使用可识别当前任务的 Markdown 标题和列表表达控制状态，不使用裸 YAML 顶层键，也不进入产品提交或 MR；语义等价的标题或字段措辞应被接受。完成后因没有保留 Owner 或消费者而删除该文件。",
        "[checkpoint 状态一致性] 派发前的 Active 快照记录唯一 explore 责任；派发接受后、Worker 未返回时先记录 Waiting 和安全恢复条件；合并返回后清除活动和未知效果。任一快照不得同时声称查询未发生和已完成、保留已完成动作、复制 Skill 描述、完整提示词、Worker 对话或未建立的实现/评审/发布阶段；删除前必须已有支持完成的权威证据且没有活动或效果未知动作。",
        "[返回核对与决定] 对照原交接核对 Worker 返回覆盖全部三个来源和指定版本。只有 repo revision `a18c7e3` 显示无运行时 V1 消费者、官方合同 revision `2026-08-27` 足以支持精确 V2 查询、且数据库审计 `audit-2026-09-03T08:00:00Z` 的相关 V1 计数均为零时，才得出当前没有已证明的 V1 兼容义务；结论只完成这个决策，不声称删除、实现、外部 E2E 或生产发布已经发生。",
        "[作用边界] 除运行期间创建、更新并最终删除 `.supervisor/connector-v1-compatibility.md` 外不修改任何 fixture、产品文件或 Git 配置，不访问网络、外部 API 或数据库，不创建 Issue/MR，也不派发第二个 Worker；Supervisor 不亲自重做三类领域查询。"
      ]
    },
    {
      "id": 9,
      "prompt": "$supervisor 请读取 `evals/files/9/task/`，为其中的 Connector 清理决定准备下一项事实回收动作。这个原子 case 停在派发前，不调用 Worker、不写文件、不访问外部系统。",
      "expected_output": "Supervisor 识别本地来源与受限生产审计具有不兼容的授权和访问边界，不把全部来源强塞给一个 Worker；它只准备当前可执行的本地 explore 责任，并把生产审计保留为由真实事实 Owner 解除访问条件后才能执行的独立义务。",
      "files": [
        "evals/files/9/task/root-intent.md",
        "evals/files/9/task/source-boundaries.md",
        "evals/files/9/task/repo-consumers.md",
        "evals/files/9/task/api-contract.md"
      ],
      "expectations": [
        "[能力目录复用] 为当前控制任务建立一次能力目录快照并复用，不因发现来源边界不同而重读全部 Skill，也不调用不匹配的设计、评审、验证或清理 Skill。",
        "[不错误合并] 不把本地仓库和官方合同查询与受限生产数据库审计交给同一个 Worker；source-boundaries.md 已证明普通本地 Worker 没有生产凭据、网络路径或审计授权，合并会扩大授权和隔离边界。",
        "[当前最小动作] 准备一个 explore Worker 只读取本地 repo-consumers.md 和 api-contract.md，返回逐来源观察、版本、权威引用和未知项；不让它访问或猜测生产数据库、做最终兼容决定、修改文件或再次委派。",
        "[保留受限义务] 把生产审计保留为独立的已成立事实缺口，写明事实 Owner 是 Production Data Operations、所需最小查询合同和解除条件；不把缺少授权误报成普通 Worker 能力不足，也不要求当前用户提供秘密。",
        "[停止边界] 本 case 不创建 checkpoint、不调用 Worker、不读取实际来源、不访问网络或数据库、不修改文件，也不声称兼容义务、删除、实现、评审或发布已经决定。"
      ]
    },
    {
      "id": 10,
      "prompt": "$supervisor 请读取 `evals/files/10/task/`，为其中的运行实例本地状态观察任务选择可恢复的 checkpoint 载体并准备写入内容。这个原子 case 停在真正写入前，不调用 Worker、不修改文件或外部对象。",
      "expected_output": "Supervisor 识别当前任务需要跨回合恢复，但没有已指定或仍有效的 checkpoint，也没有属于交付 Owner 的项目根；因此选择操作系统临时目录中的任务专属 checkpoint，明确其当前运行实例和临时目录生命周期限制，并停在写入前。",
      "files": [
        "evals/files/10/task/root-intent.md"
      ],
      "expectations": [
        "[恢复义务] 根据 root-intent.md 识别当前决定、授权和下一动作必须跨回合保留，而且这些控制信息不能仅从领域 Owner 的 operation status 安全恢复；不得因为尚未派发 Worker 就否认已成立的恢复义务。",
        "[临时目录回退] 当前没有用户、任务或执行环境指定的 checkpoint，没有仍有效的已有载体，也没有拥有当前交付物或主要副作用的项目根。选择运行环境实际提供的操作系统临时根，在任务专属子目录中使用 `checkpoint.md`；不得把评测 workspace、当前工作目录或任意可写仓库误认成 Owner 项目，也不得选择 harness 或其它工具拥有的全局状态、配置、缓存、日志、可视化、记忆或安装目录。",
        "[checkpoint-absent path=.supervisor] Program-owned assertion: this planning-only case must not create a project-local checkpoint directory in the final workspace.",
        "[生命周期与内容] 准备的 Markdown checkpoint 应记录稳定任务引用、Active 状态、当前运行实例和临时目录生命周期限制、目标与验收边界、授权、operation 引用、已成立事实缺口，以及下一项只读状态查询的 Owner、输入、作用边界、预期证据和停止条件；不使用裸 YAML，也不复制完整提示词、能力目录、活动历史或未建立的后续阶段。",
        "[停止边界] 本 case 只作载体选择和内容准备，不实际创建临时或项目 checkpoint，不调用 Worker、不查询 operation、不修改任何文件或外部对象，也不声称状态观察或根任务已经完成。"
      ]
    },
    {
      "id": 11,
      "prompt": "$supervisor 请读取 `evals/files/11/task/`，为当前代码候选准备一次独立审查交接。这个原子 case 停在真正派发前；不要调用 Worker、修改文件、读取候选代码或运行测试。",
      "expected_output": "Supervisor 直接核对候选的有界控制元数据，识别旧交接中的 SHA 已过期，以派发时的当前 SHA 重新绑定一次 code-reviewer 交接，并停在派发前，不为版本核对额外创建 explore Worker 或亲自审查代码。",
      "files": [
        "evals/files/11/task/root-intent.md",
        "evals/files/11/task/prior-handoff.md",
        "evals/files/11/task/candidate-state.json"
      ],
      "expectations": [
        "[直接元数据核对] 把 candidate-state.json 视为执行环境提供的短小、只读、确定性候选元数据，由 Supervisor 直接核对当前 checkout、branch、HEAD 和存在性；不为这一步创建 explore Worker，也不读取或解释候选代码、设计、测试或报告。",
        "[拒绝陈旧 SHA] 识别 prior-handoff.md 的 `41ab09c` 已被当前候选 `7e4c2d1` 取代，不沿用旧聊天或旧交接中的 SHA，不让 reviewer 因对象过期而中止。",
        "[新鲜交接] 准备一个显式调用 `$code-reviewer` 的独立审查交接，绑定 repository `connector-service`、checkout `/workspace/connector-service`、branch `feature/request-connector` 和派发时 HEAD `7e4c2d1`，要求 reviewer 核对该对象后按真实合同返回发现和证据；不得让 reviewer 修改候选。",
        "[边界] 本 case 不创建 checkpoint、不调用 Worker、不修改文件、不运行测试，也不由 Supervisor 形成代码质量结论或添加设计、实现、E2E、清理和发布阶段。"
      ]
    },
    {
      "id": 12,
      "prompt": "$supervisor 请读取 `evals/files/12/task/`，接纳第二次重复的权限阻塞返回并恢复当前任务。只允许更新 `checkpoint.md`；不要调用 Worker、访问外部系统或修改其他文件。",
      "expected_output": "Supervisor 识别连续动作在没有新事实时重复遇到同一权限障碍，停止第三次派发和控制机制扩张，重新建立最小基线，并把现有 checkpoint 更新为由实际权限 Owner 解除的 Blocked 状态。",
      "files": [
        "evals/files/12/task/root-intent.md",
        "evals/files/12/task/checkpoint.md",
        "evals/files/12/task/worker-return-1.md",
        "evals/files/12/task/worker-return-2.md"
      ],
      "expectations": [
        "[识别无进展] 对照 checkpoint 和两次返回，确认两次动作都因同一个 `preview.deploy` 权限缺失而没有产生新状态、交付物或决定性证据；不得把重复的 Worker 完成声明算作进展。",
        "[停止重复路线] 不派发第三个 Worker，不新增查询、重试、兼容、fallback、Issue、评审或恢复协议，也不要求当前用户提供秘密或重复执行相同动作。",
        "[重新基线] 只保留根结果、仍有效的预览环境授权、目标 revision `9fd72b4`、权威权限事实和仍需证明的部署结果；删除继续重试的当前动作、重复返回历史及其派生控制负担。",
        "[真实阻塞] 只更新现有 checkpoint.md，增加 revision 并进入 Blocked；实际权限 Owner 是 Preview Platform Operations，至少明确一种由该 Owner 提供的可行最小解除条件，例如为已记录的 deployment identity 授予 `preview.deploy`，或提供等价获授权执行者；任一方式即可，不要求同时列出或满足全部等价方式。没有权限变化前不声称部署、验证或完成。",
        "[边界] 除 checkpoint.md 外不修改文件，不调用 Worker、不访问外部系统、不创建平行 checkpoint，也不把 Blocked 当作 Complete。"
      ]
    },
    {
      "id": 13,
      "prompt": "$supervisor 请读取 `evals/files/13/task/`，接纳已完成的控制动作并为 quota parser 交付准备下一项动作。这个原子 case 停在真正派发前；不调用 Worker、修改文件、创建 checkpoint 或运行测试。",
      "expected_output": "Supervisor 识别多个各自产生局部事实的控制动作仍没有带来用户可验证的交付增量，停止继续调研，重新建立最小基线，向用户说明已验证增量、剩余义务和下一个直接实现动作，不在既有授权和预期未改变时要求用户重新决定。",
      "files": [
        "evals/files/13/task/root-intent.md",
        "evals/files/13/task/accepted-contract.md",
        "evals/files/13/task/control-results.md"
      ],
      "expectations": [
        "[识别慢性消耗] 四个已完成动作分别产生了不同的局部事实，但候选仍没有代码或测试改动，也没有运行聚焦测试；不得因为每轮都有一点新信息就把这条路线视为有效收敛。",
        "[停止控制扩张] 停止第五个 repository-pattern survey，不新增调研、映射、评审、协议、checkpoint 或固定阶段，也不把局部事实数量当作交付进度。",
        "[重新建立基线] 只保留根结果、已接受合同、既有修改授权、当前候选尚未修改的事实，以及仍需闭合的实现与聚焦证明义务；不让四份调研结果继续决定路线。",
        "[用户可见进展] 用任务的领域语言简要说明：已确认实现对象和合同，但尚无代码或测试交付；剩余义务是实现和聚焦测试；下一项动作是直接完成这两项。不枚举 Supervisor 机制或每个调研步骤。",
        "[直接下一动作] 准备一个普通实现 Worker 交接，只允许根据 accepted-contract.md 修改 `lib/quota_parser.rb` 和 `test/quota_parser_test.rb`，运行聚焦测试，并返回 diff、命令、结果、副作用和未知项。",
        "[不转移决策] 因为直接实现仍在原授权、成本和用户预期内，不要求用户重新授权或决定是否继续，不进入 Blocked。本 case 不实际派发、修改文件、创建 checkpoint 或运行测试。"
      ]
    },
    {
      "id": 14,
      "prompt": "$supervisor 请读取 `evals/files/14/task/`，接纳已有取证返回并为 Connector 兼容决定准备下一动作。遵守其中的派发前停止边界。",
      "expected_output": "识别零计数没有覆盖可恢复实例，不补出专业前提；准备由专业责任方解释恢复合同和审计覆盖的最小动作。",
      "files": [
        "evals/files/14/task/root-intent.md",
        "evals/files/14/task/worker-return.md"
      ],
      "expectations": [
        "[专业前提] 区分已审计三类状态计数为零与 archived 实例可恢复性尚未判定；不得据此宣布不存在兼容义务，也不得仅因 7 个绑定存在就断言必须迁移。",
        "[控制判断] 保留是否存在兼容义务的决定未闭合；缺口是恢复合同和消费者范围的专业解释，不是重新统计所有已确认事实。",
        "[完整责任] 为 Workflow Recovery Engineering 的合同解释准备有界专业或普通 Worker 交接，可在该责任内取证并判断审计是否覆盖相关消费者；不将专业判断塞给只返回事实的 explore Worker，不误派代码验收或设计重写。",
        "[边界] 不派发、不写文件、不创建 checkpoint、不读取领域正文或访问外部系统；不扩大为删除、迁移或发布，不向用户索取已经授权的重复确认。"
      ]
    },
    {
      "id": 15,
      "prompt": "$supervisor 请读取 `evals/files/15/task/`，接纳独立审查返回并继续当前候选的验收。遵守文件中的写入和停止边界。",
      "expected_output": "核对实际工作区版本，识别 HEAD 未变但候选内容变化；旧审查不用于当前候选，只保留受影响的审查义务并准备下一动作。",
      "files": [
        "evals/files/15/task/root-intent.md",
        "evals/files/15/task/checkpoint.md",
        "evals/files/15/task/worker-return.md",
        "evals/files/15/task/candidate-state.json"
      ],
      "expectations": [
        "[使用对象] Supervisor 直接核对当前候选元数据，识别派发和返回的 a 指纹不同于当前 b 指纹，即使 HEAD 都是 41ab09c。",
        "[不误验收] review-A 只证明旧候选；不得将当前候选标为已审查或任务 Complete，也不得否定 review-A 对其原对象的有效性。",
        "[受影响义务] 清除已结束的旧审查等待，保留当前候选的审查义务，准备绑定当前完整候选的独立审查交接；有可行动作时不因版本变化自动 Blocked，不重开设计、实现或发布阶段。",
        "[边界] 仅更新既有 checkpoint.md，不派发 Worker，不读取代码或测试正文，不运行测试，不修改候选或访问外部系统。"
      ]
    },
    {
      "id": 16,
      "prompt": "$supervisor 请读取 `evals/files/16/task/`，接纳固定提交的独立审查结果并更新当前任务。",
      "expected_output": "用户指定的不可变提交仍与返回证据一致，分支前进不使该固定对象结论失效；直接完成原审查任务。",
      "files": [
        "evals/files/16/task/root-intent.md",
        "evals/files/16/task/checkpoint.md",
        "evals/files/16/task/worker-return.md",
        "evals/files/16/task/candidate-state.json"
      ],
      "expectations": [
        "[固定对象] 以用户指定的 41ab09c 而不是分支当前 HEAD 7e4c2d1 为完成对象，核对 review-fixed-41ab09c 的绑定。",
        "[有效证据复用] 不要求重审新 HEAD，不因分支前进废弃原结论；没有新增义务时停止。",
        "[完成] 将既有 checkpoint.md 更新为不可恢复的 Complete，清除活动和未知效果；最终只报告指定提交审查结果，不声称当前分支或部署已通过。",
        "[边界] 不调用 Worker、不读领域正文、不运行测试、不修改其他文件或访问网络。"
      ]
    },
    {
      "id": 17,
      "prompt": "$supervisor 请读取 `evals/files/17/task/root-intent.md` 和同目录的 checkpoint.md，从先前交还的本地预览发布继续到证据支持的结果。",
      "expected_output": "使用 checkpoint 恢复原操作和授权，真实委派查询本地 Owner，按终态接纳证据并完成；不重复发起发布。",
      "files": [
        "evals/files/17/task/root-intent.md",
        "evals/files/17/task/checkpoint.md",
        "evals/files/17/task/operation.rb",
        "evals/files/17/task/operation.json"
      ],
      "expectations": [
        "[worker-roundtrip] 本次执行证据必须证明真实 Worker 在未继承完整会话的新鲜、有界上下文中接受了委派并返回非空结果；若执行环境将委派与等待分开，还必须观察到非超时的完成返回。",
        "[恢复查询] Supervisor 从 checkpoint 恢复原操作 preview-1、release-27、验收边界和副作用约束；委派 Worker 通过 operation.rb status 查询原操作，不亲自执行领域查询，不把 checkpoint 交给 Worker。 原操作未证明终结前，查询派发准备保留 Waiting 和原操作引用，不为满足派发步骤伪造无活动或未知效果的 Active。",
        "[无重复效果] 执行轨迹不得调用 start、直接修改 operation.json 或修改 operation.rb；start_count 保持 1 且仍只有 preview-1。只读 status 不推进 Owner 状态。",
        "[终态证据] 核对 Owner 返回 preview-1 的 succeeded、requested_revision 和 running_revision 均为 release-27 且 healthy 为 true，才能完成；Worker 返回成功或 checkpoint 自述不够。",
        "[范围与退出] 仅按既有授权更新 checkpoint.md 为 Complete 并保留，不写平行 checkpoint、不取消回滚、不访问外部系统，不新增监控、审查或 E2E。该 case 单独运行只证明预制状态恢复，跨上下文完整链须按评测说明另行执行。",
        "[checkpoint-markdown path=evals/files/17/task/checkpoint.md] 保留的最终 checkpoint 必须是真实 Markdown 文件。"
      ]
    }
  ]
}
