interface:
  display_name: "Code Simplifier"
  short_description: "Reduce and restructure a bounded code candidate"
  default_prompt: "Use $code-simplifier to reduce this bounded code candidate's maintenance burden while preserving its decided behavior and contracts; make no edit when no justified improvement is needed."
policy:
  allow_implicit_invocation: false
